using System.Linq;

namespace NFAParser
{
    internal class NFA
    {
        internal bool TestWord(Automaton automaton, string word, string currentNode)
        {
            var startingNode = automaton.StartingPoint;
            foreach (var c in word)
            {

            }
        }
    }
}
